{
  "name": "@apps/frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test",
    "lint": "tsc -p tsconfig.json --noEmit",
    "format": "echo 'No formatter configured for @apps/frontend'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@packages/types": "0.1.0"
  }
}
