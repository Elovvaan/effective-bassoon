{
  "name": "@packages/ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test",
    "lint": "tsc -p tsconfig.json --noEmit",
    "format": "echo 'No formatter configured for @packages/ui'",
    "clean": "rm -rf dist"
  },
  "exports": "./src/index.ts"
}
